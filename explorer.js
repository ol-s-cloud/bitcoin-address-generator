(()=>{const tabs=[...document.querySelectorAll('.network-tab')];const state={testnet:{addresses:0,transactions:0,received:'0 BTC',active:0},mainnet:{addresses:0,transactions:0,received:'0 BTC',active:0}};function render(network){const data=state[network];document.getElementById('exAddresses').textContent=data.addresses;document.getElementById('exTransactions').textContent=data.transactions;document.getElementById('exReceived').textContent=data.received;document.getElementById('exActive').textContent=data.active;document.getElementById('explorerActivity').innerHTML=`No public COBRA addresses recorded on Bitcoin ${network==='testnet'?'Testnet':'Mainnet'} yet.<br><small>${network==='testnet'?'This is where the first COBRA test address and its public transactions will appear.':'Mainnet remains at zero until deliberate small-value validation follows testnet testing.'}</small>`}tabs.forEach(tab=>tab.addEventListener('click',()=>{tabs.forEach(x=>x.classList.toggle('active',x===tab));render(tab.dataset.network)}));render('testnet')})();