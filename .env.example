# Server-only Neon Postgres connection string.
# In Vercel, the Neon integration normally supplies DATABASE_URL automatically.
DATABASE_URL=

# Server-only 32-byte key encoded as base64.
# Used to encrypt supplier/API credentials at rest. Never expose this to browser code.
COBRA_CREDENTIAL_ENCRYPTION_KEY=
